---

machine:
  python:
    version: 2.7.6

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install python-vtk
    - pip install pytest

test:
  override:
    - py.test:
        environment:
          PYTHONPATH: "$PYTHONPATH:$HOME/chagu"
